ktor {
    deployment {
        port = ${PORT}
    }
    application {
        modules = [ com.itis.timetable.ApplicationKt.module ]
    }
}

postgres {
    url = ${DATABASE_URL}
    user = ${PGUSER}
    password = ${PGPASSWORD}
}